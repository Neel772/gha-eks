name: Kubernetes CI
on:
  push:
    branches:
      - main

jobs:
  pyapp-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Code checkout
        uses: actions/checkout@7
      - name: Build Docker Image
        run: "docker build -t nk772/gha-pyapp:$GITHUB.SHA ."
